// These MUST MATCH the names of the stores inside of the persisted data store!
// There is a way to enforce this, but would need to export the WotdlePersistedDataStore type or move the definition here. Worth it?

export enum GameType {
    Classic = "classic",
    Hard = "hard",
}

